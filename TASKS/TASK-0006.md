# TASK-0006 — Governance Protection in MutationEngine.apply()

## Goal

Prevent MutationFlow from modifying governance/control files unless an explicit override is present.

## Scope (files allowed to touch)

- project_guardian/mutation.py (MutationEngine class)
- SPEC.md (only if you must list protected paths there; prefer code constant)
- tests/test_invariants.py (only if needed to match actual paths)
- REPORTS/AGENT_REPORT.md
- CHANGELOG.md
- TASKS/TASK-0006.md
- CONTROL.md

## Non-goals

- No major redesign of MutationFlow
- No feature creep into "full RBAC" or policy engines

## Requirements

1. Define protected paths (minimum):
   - CONTROL.md
   - SPEC.md
   - any file under: TASKS/, REPORTS/, scripts/, tests/ (optional but recommended)
   - core governance module files (IdentityAnchor, TrustEngine, MutationFlow, PromptRouter, ElysiaLoop-Core)

2. MutationEngine.apply() must reject any mutation touching protected paths unless:
   - an explicit override flag is passed (e.g., allow_governance_mutation=True)
   - AND the caller is authorized (at minimum: TrustMatrix approval)

3. Rejection must be explicit (exception or structured result), with a clear reason.

## Acceptance

- `scripts/acceptance.ps1` passes
- Mutation protection invariant passes
- Demonstrate rejection path in AGENT_REPORT
- CONTROL.md set back to CURRENT_TASK: NONE

## Rollback

Revert changes.
