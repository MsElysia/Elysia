# TASK-0011 — Define External Action Gateway Interfaces

## Goal

Define explicit gateway modules/classes for filesystem and subprocess actions so policy enforcement is centralized.

## Scope

- project_guardian/* (new gateway modules only)
- tests/test_invariants.py
- REPORTS/AGENT_REPORT.md
- CHANGELOG.md
- TASKS/TASK-0011.md
- CONTROL.md

## Requirements

1. Add FileWriter gateway with TrustMatrix gating contract (no broad refactors; just add module + stubs).

2. Add SubprocessRunner gateway with TrustMatrix gating contract (stubs acceptable).

3. Update bypass detection allowlist to permit these gateways only.

## Acceptance

- Tests pass
- No external writes/subprocess allowed outside gateways
