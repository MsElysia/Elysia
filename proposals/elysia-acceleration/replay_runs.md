# Replay pack — run log (optional)

Append one row per weekly session (date, prompts run, notes on `selfbuild_rag` / answer quality).

| Date (local) | Pack version | Notes |
|--------------|--------------|-------|
| *(baseline)* | replay_prompts.jsonl | Initial template; fill after first run. |
